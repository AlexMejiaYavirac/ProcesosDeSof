namespace Mocks
{
    internal interface ICalcular
    {

        int obtenerMayor(int numero1, int numero2);
        int obtenerAreaRectangulo(int numero1, int numero2);
        int obtenerAreaTriangulo(int numero1, int numero2);
        double obtenerIva(int numero1, int numero2);
        int obtenerResta(int numero1, int numero2);
    }
}